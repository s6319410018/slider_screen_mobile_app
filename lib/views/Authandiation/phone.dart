import 'package:flutter/material.dart';

class PHONE_UI extends StatefulWidget {
  const PHONE_UI({super.key});

  @override
  State<PHONE_UI> createState() => _PHONE_UIState();
}

class _PHONE_UIState extends State<PHONE_UI> {
  @override
  Widget build(BuildContext context) {
    return Card(
      color: Colors.amber,
    );
  }
}
